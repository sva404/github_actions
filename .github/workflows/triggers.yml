name: triggers

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
      - 'releases/**'
    paths:
      '**.cpp'
  schedule:
    - cron: '* * 26 * *'

jobs: 
  hola-mundo:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: imprimir hola github
        run: echo "hola github"
